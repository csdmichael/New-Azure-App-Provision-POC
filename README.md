# New-Azure-App-Provision-POC
This project demonstrates how an Enterprise can expose a centralized GitHub Workflow framework to provision new Applications on Azure that have a separate resource group by environment and an App Registration per environment where manifest has environment name, cost center and other parameters to help chargeback cost by cost center and user
